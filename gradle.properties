# Root gradle.properties to ensure CI uses desired Kotlin/DSL flags
# This file forces values so plugin logic doesn't pick up conflicting flags from runner-global config.
org.gradle.vfs.watch=false

# Ensure built-in Kotlin flags do not trigger legacy plugin behavior
android.builtInKotlin=false
android.newDsl=true
