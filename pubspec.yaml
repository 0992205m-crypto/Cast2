name: cast2
description: "Ultimate Web Video Caster App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # تم إضافة هذا السطر لإصلاح خطأ السيرفر تماماً

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  video_player: ^2.9.2
  flutter_cast_video: ^2.0.0
  dlna_dart: ^0.1.3
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
