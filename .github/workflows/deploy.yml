git clone https://github.com/0992205m-crypto/Cast2.git
cd Cast2
git checkout -b ci/patch-inappwebview
# حرر .github/workflows/deploy.yml وأدخل المقطع أعلاه
git add .github/workflows/deploy.yml
git commit -m "ci: patch flutter_inappwebview_android build.gradle temporarily in workflow to avoid CI evaluation error"
git push origin ci/patch-inappwebview
# افتح PR وادمجه أو ادفع مباشرة إلى main إذا لديك صلاحية
